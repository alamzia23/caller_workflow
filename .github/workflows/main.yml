name: Caller Workflow

on:
  pull_request:
    types: [opened, closed]

jobs:
  call-reusable-workflow:
    uses: alamzia23/reusable_Workflow_repo/.github/workflows/reusable.yml@main
    with:
      greeting: Hello from the caller workflow!
